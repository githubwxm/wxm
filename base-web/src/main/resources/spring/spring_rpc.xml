<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:dubbo="http://code.alibabatech.com/schema/dubbo" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:context="http://www.springframework.org/schema/context"
	xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
	http://code.alibabatech.com/schema/dubbo        http://code.alibabatech.com/schema/dubbo/dubbo.xsd http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd"
	default-autowire="byName">


	<!-- 远程rpc类声明 -->
	<!-- 提供方应用信息，用于计算依赖关系 -->
	<dubbo:application name="base-web" />

	<!---->
	<dubbo:registry protocol="zookeeper" address="${dubbo.registry.zk}" /> 
	<dubbo:protocol name="dubbo" port="${dubbo.port}" /> 
	
	<!-- 声明需要暴露的服务接口 -->
	<!--dubbo:service interface="com.all580.base.rpc.interfaces.UserRPCService"
		delay="-1" ref="userRPCService" /-->

	<!-- 和本地bean一样实现服务 -->
	<!--bean id="userRPCService" class="com.all580.base.service.UserService" /-->
	
</beans>